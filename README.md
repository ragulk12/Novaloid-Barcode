# Novaloid-Barcode

A React Native application built with Expo.

## 🚀 Getting Started

Follow these steps to run the project locally.

### 1️⃣ Clone the repository
```bash
git clone https://github.com/ragulk12/Novaloid-Barcode.git
npm install --force
npm start
the app will run on expo
